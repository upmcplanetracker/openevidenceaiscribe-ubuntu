# ===== CLINIC AUDIO ROUTER CONFIG =====

# Headset brand or unique name (used for mic + headphones)
HEADSET_NAME="Jabra"

# Zoom PipeWire node name
ZOOM_NAME="ZOOM VoiceEngine"

# Chrome PipeWire node name
CHROME_NAME="Google Chrome"

# Chrome recording input (only exists while recording)
CHROME_INPUT_NAME="Chrome input:input_MONO"

# Virtual sink name (do not change unless needed)
SCRIBE_SINK="Scribe_Mixer"
